---
# Example inventory for external project
all:
  hosts:
    docker-server:
      ansible_host: 192.168.1.100
      ansible_user: ansible
      ansible_ssh_private_key_file: ~/.ssh/id_rsa

  vars:
    ansible_become: true
    ansible_become_password: "{{ vault_become_password }}"
