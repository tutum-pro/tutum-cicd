---
# Tutum Admin CLI Installation Playbook (Docker)
#
# Usage:
#   ansible-playbook tutum_pro.cicd.cli

- name: Install Tutum Admin CLI (Docker)
  hosts: all
  become: true
  roles:
    - role: tutum_pro.cicd.docker.tutum_cli
