---
# Tutum Engine Installation Playbook (Docker)
#
# Usage:
#   ansible-playbook tutum_pro.cicd.engine

- name: Install Tutum Engine (Docker)
  hosts: all
  become: true
  roles:
    - role: tutum_pro.cicd.docker.tutum_engine
