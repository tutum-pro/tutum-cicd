---
# Ansible Galaxy requirements for external project
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  # Install from Galaxy (after publishing)
  - name: tutum_pro.cicd
    version: ">=1.2.4"

  # Or install from git (before publishing to Galaxy)
  # - name: https://github.com/tutum-pro/tutum-cicd.git
  #   type: git
  #   version: main
